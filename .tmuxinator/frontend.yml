name: frontend
root: ~/data/versioning/gitlab/serene-core/serene/EngineFrontend

tmux_options: -2
windows:
  - vim: 
    - v app
  - shell:
    - alias cdr="cd $(pwd)"
  - npm:
    - npm start
  - guard:
    - guard
