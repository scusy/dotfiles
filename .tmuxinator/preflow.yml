
# ~/.tmuxinator/mlpipe-config.yml

name: preflow
root: ~/preflow/

tmux_options: -2
pre_window: alias cdr="cd $(pwd)"
windows:
  - vim: 
    - v 
  - preflow: cd preflow/preflow
  - tagging: cd preflow-tagging-webapp

